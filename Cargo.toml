[package]
name = "plato-training-casino"
version = "0.1.0"
edition = "2021"
description = "Training casino — multi-armed bandit, adaptive sampling, exploration-exploitation strategies"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
